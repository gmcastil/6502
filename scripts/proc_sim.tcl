# Simulation requires an on-disk project

set build_dir "../build"
set proj_name "proc-sim"
set part "xc7a35ticpg236-1L"

# Create an empty project in the build directory
create_project $proj_name $build_dir/$proj_name -part $part

# Check for existence of memory block and if not, create one

# Import sources

# Run simulation

# Create new waveform
